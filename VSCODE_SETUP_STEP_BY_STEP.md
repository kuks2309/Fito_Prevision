# VSCode 설정 단계별 가이드

## 1단계: 확장 프로그램 설치 확인

### 방법 1: 확장 프로그램 뷰에서 확인

1. **확장 프로그램 뷰 열기**
   ```
   Ctrl+Shift+X
   ```

2. **필수 확장 프로그램 설치**

   #### a) C/C++ (Microsoft)
   ```
   검색: "C++"
   → "C/C++" (작성자: Microsoft) 설치
   → "Install" 버튼 클릭
   ```

   #### b) CMake Tools (Microsoft)
   ```
   검색: "CMake Tools"
   → "CMake Tools" (작성자: Microsoft) 설치
   → "Install" 버튼 클릭
   ```

   #### c) CMake (twxs)
   ```
   검색: "CMake"
   → "CMake" (작성자: twxs) 설치
   → "Install" 버튼 클릭
   ```

3. **설치 확인**
   - 확장 프로그램 뷰 왼쪽 사이드바에서 "Installed" 탭 클릭
   - 위 3개 확장 프로그램이 보이는지 확인

### 방법 2: extensions.json 사용 (자동 설치)

1. **프로젝트 열기**
   ```
   File → Open Folder → D:\FITO_2026\Prevision
   ```

2. **우측 하단 알림 확인**
   - "This workspace has extension recommendations." 알림이 나타나면
   - "Install All" 또는 "Show Recommendations" 클릭

3. **수동으로 권장 확장 프로그램 보기**
   ```
   Ctrl+Shift+P → "Extensions: Show Recommended Extensions" 입력
   ```

## 2단계: CMake Tools 활성화 확인

### CMake Tools가 설치되었는지 확인

1. **명령 팔레트 열기**
   ```
   Ctrl+Shift+P
   ```

2. **"cmake" 입력**
   - CMake 관련 명령어가 보이는지 확인
   - 보이는 명령어 예시:
     - CMake: Configure
     - CMake: Build
     - CMake: Select a Kit
     - CMake: Scan for Kits

3. **명령어가 안 보이면**
   - VSCode 재시작
   - 확장 프로그램 재설치

## 3단계: Kit 검색 및 선택

### 방법 1: 자동 검색

1. **Kit 스캔**
   ```
   Ctrl+Shift+P → "CMake: Scan for Kits" 입력 → 엔터
   ```
   - Visual Studio, MinGW, GCC 등 설치된 컴파일러를 자동 검색

2. **Kit 선택**
   ```
   Ctrl+Shift+P → "CMake: Select a Kit" 입력 → 엔터
   ```
   - 목록에서 "Visual Studio Community 2019 Release - amd64" 선택

### 방법 2: 상태바 사용

CMake Tools가 활성화되면 VSCode 하단 상태바에 다음과 같이 표시됩니다:

```
[No Kit Selected] [Build] [Run]
```

1. **[No Kit Selected] 클릭**
   - Kit 선택 드롭다운이 나타남
   - "Visual Studio Community 2019 Release - amd64" 선택

### 방법 3: 수동으로 cmake-tools-kits.json 생성

Kit이 자동 감지되지 않으면 수동으로 생성:

1. **파일 생성**
   ```
   C:\Users\[사용자이름]\AppData\Local\CMakeTools\cmake-tools-kits.json
   ```

2. **내용 작성**
   ```json
   [
     {
       "name": "Visual Studio Community 2019 Release - amd64",
       "visualStudio": "VisualStudio.16.0",
       "visualStudioArchitecture": "x64",
       "preferredGenerator": {
         "name": "Visual Studio 16 2019",
         "platform": "x64"
       }
     }
   ]
   ```

3. **VSCode 재시작**

## 4단계: CMake 설정 (Configure)

### 방법 1: 명령 팔레트

1. **CMake Configure 실행**
   ```
   Ctrl+Shift+P → "CMake: Configure" 입력 → 엔터
   ```

2. **Kit 선택 (처음 실행 시)**
   - "Visual Studio Community 2019 Release - amd64" 선택

3. **Output 확인**
   ```
   View → Output (Ctrl+Shift+U)
   드롭다운에서 "CMake/Build" 선택
   ```

### 방법 2: 자동 설정

프로젝트를 열면 자동으로 Configure가 실행됩니다 (settings.json에 설정됨):
```json
{
    "cmake.configureOnOpen": true
}
```

## 5단계: 빌드

### 방법 1: 단축키
```
F7 또는 Ctrl+Shift+B
```

### 방법 2: 명령 팔레트
```
Ctrl+Shift+P → "CMake: Build" 입력 → 엔터
```

### 방법 3: 상태바
```
하단 상태바에서 [⚙️ Build] 버튼 클릭
```

## 6단계: 실행

### 방법 1: 단축키
```
Ctrl+F5 (디버그 없이)
F5 (디버그 모드)
```

### 방법 2: 명령 팔레트
```
Ctrl+Shift+P → "CMake: Run Without Debugging" 입력 → 엔터
```

### 방법 3: 상태바
```
하단 상태바에서 [▶️ Run] 버튼 클릭
```

## 문제 해결

### 문제 1: "CMake: Select a Kit"이 안 보임

**원인**: CMake Tools 확장 프로그램 미설치

**해결**:
1. Ctrl+Shift+X
2. "CMake Tools" 검색
3. Microsoft 작성자의 "CMake Tools" 설치
4. VSCode 재시작

### 문제 2: Kit 목록이 비어있음

**원인**: Visual Studio가 설치되지 않았거나 감지되지 않음

**해결**:
1. Visual Studio 2019 설치 확인
2. `Ctrl+Shift+P → "CMake: Scan for Kits"`
3. VSCode 재시작
4. 여전히 안 되면 수동으로 cmake-tools-kits.json 생성 (위 참고)

### 문제 3: CMake 명령어가 전혀 안 보임

**원인**: CMake가 시스템에 설치되지 않음

**해결**:
1. CMake 다운로드: https://cmake.org/download/
2. 설치 시 "Add CMake to system PATH" 체크
3. 터미널에서 확인: `cmake --version`
4. VSCode 재시작

### 문제 4: 상태바에 CMake 관련 버튼이 안 보임

**원인**: CMakeLists.txt 파일이 루트에 없거나 인식 안 됨

**해결**:
1. 프로젝트 루트에 CMakeLists.txt 파일 확인
2. `Ctrl+Shift+P → "CMake: Configure"`
3. 프로젝트 폴더 재열기

## CMake Tools 없이 빌드하는 방법 (대안)

CMake Tools 설치가 어렵다면 터미널을 사용하세요:

### 1. VSCode 터미널 열기
```
Ctrl+`
```

### 2. CMake 설정
```bash
cmake -B build -G "Visual Studio 16 2019" -A x64
```

### 3. 빌드
```bash
cmake --build build --config Release
```

### 4. 실행
```bash
cd build\Release
.\VisionComparison.exe
```

## 확인 체크리스트

프로젝트를 제대로 설정했는지 확인:

- [ ] CMake 설치됨 (`cmake --version` 확인)
- [ ] Visual Studio 2019 설치됨
- [ ] VSCode 확장 프로그램 설치:
  - [ ] C/C++ (Microsoft)
  - [ ] CMake Tools (Microsoft)
  - [ ] CMake (twxs)
- [ ] 프로젝트 폴더 열림 (`D:\FITO_2026\Prevision`)
- [ ] CMakeLists.txt 파일 존재 (루트)
- [ ] `.vscode` 폴더 존재
- [ ] Kit 선택됨 (상태바 확인)
- [ ] CMake Configure 성공
- [ ] 빌드 성공
- [ ] 실행 성공

## 스크린샷 가이드

### 확장 프로그램 설치 화면
```
┌─────────────────────────────────────────┐
│ Extensions: Marketplace                 │
├─────────────────────────────────────────┤
│ 🔍 CMake Tools                          │
├─────────────────────────────────────────┤
│ 📦 CMake Tools                          │
│    Microsoft                            │
│    [Install] 버튼                       │
├─────────────────────────────────────────┤
│ 📦 C/C++                                │
│    Microsoft                            │
│    [Install] 버튼                       │
└─────────────────────────────────────────┘
```

### 상태바 (CMake Tools 활성화 후)
```
┌─────────────────────────────────────────────────────────┐
│ [🔧 Visual Studio 2019] [Debug▼] [⚙️Build] [▶️Run]    │
└─────────────────────────────────────────────────────────┘
```

### 명령 팔레트 (Ctrl+Shift+P)
```
┌─────────────────────────────────────────┐
│ > cmake                                 │
├─────────────────────────────────────────┤
│ CMake: Configure                        │
│ CMake: Build                            │
│ CMake: Select a Kit                     │
│ CMake: Scan for Kits                    │
│ CMake: Run Without Debugging            │
└─────────────────────────────────────────┘
```

## 추가 도움말

### CMake Tools 공식 문서
https://github.com/microsoft/vscode-cmake-tools/blob/main/docs/README.md

### VSCode C++ 개발 가이드
https://code.visualstudio.com/docs/cpp/config-msvc

### 문제가 계속되면
1. VSCode 재시작
2. 컴퓨터 재시작
3. 확장 프로그램 재설치
4. CMake 재설치

그래도 안 되면 터미널 방식으로 빌드하세요! (위 "CMake Tools 없이 빌드하는 방법" 참고)
